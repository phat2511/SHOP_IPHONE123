@model SHOP_IPHONE.Models.ProductVariant

@{
    ViewBag.Title = "Thêm biến thể mới";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">
                        <i class="fa fa-plus-circle"></i> Thêm biến thể mới
                    </h3>
                    <p class="text-muted mb-0">Sản phẩm: <strong>@ViewBag.Product.product_name</strong></p>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("Create", "ProductVariant", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.product_id)

                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label class="form-label">
                                        <i class="fa fa-palette"></i> Màu sắc <span class="text-danger">*</span>
                                    </label>
                                    <select name="color" class="form-control" required>
                                        <option value="">-- Chọn màu sắc --</option>
                                        @foreach (var color in ViewBag.AvailableColors)
                                        {
                                            <option value="@color">@color</option>
                                        }
                                    </select>
                                    @Html.ValidationMessageFor(model => model.color, "", new { @class = "text-danger" })
                                </div>

                                <div class="form-group mb-3">
                                    <label class="form-label">
                                        <i class="fa fa-money-bill"></i> Giá cho màu này <span class="text-danger">*</span>
                                    </label>
                                    <div class="input-group">
                                        @Html.TextBoxFor(model => model.price, new {
                                            @class = "form-control",
                                            type = "number",
                                            placeholder = "Nhập giá (VNĐ)",
                                            required = "required"
                                        })
                                        <span class="input-group-text">VNĐ</span>
                                    </div>
                                    @Html.ValidationMessageFor(model => model.price, "", new { @class = "text-danger" })
                                </div>

                                <div class="form-group mb-3">
                                    <label class="form-label">
                                        <i class="fa fa-boxes"></i> Số lượng <span class="text-danger">*</span>
                                    </label>
                                    @Html.TextBoxFor(model => model.stock, new
                                    {
                                        @class = "form-control",
                                        type = "number",
                                        placeholder = "Nhập số lượng",
                                        required = "required"
                                    })
                                    @Html.ValidationMessageFor(model => model.stock, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label class="form-label">
                                        <i class="fa fa-image"></i> Ảnh cho màu này <span class="text-danger">*</span>
                                    </label>
                                    <input type="file" name="imageFile" id="imageFile" class="form-control" accept="image/*" required />
                                    <small class="form-text text-muted">
                                        <i class="fa fa-info-circle"></i>
                                        Chọn ảnh hiển thị cho màu sắc này (JPG, PNG, GIF)
                                    </small>
                                </div>

                                <div id="imagePreview" class="mt-3" style="display: none;">
                                    <label class="form-label">Xem trước ảnh:</label>
                                    <div class="border rounded p-2">
                                        <img id="preview" src="#" alt="Preview" class="img-fluid" style="max-height: 200px; width: 100%; object-fit: cover;" />
                                    </div>
                                </div>

                                <div class="alert alert-info mt-3">
                                    <i class="fa fa-lightbulb"></i>
                                    <strong>Gợi ý:</strong>
                                    <ul class="mb-0 mt-2">
                                        <li>Chọn ảnh chất lượng cao để hiển thị tốt</li>
                                        <li>Kích thước ảnh nên từ 300x300px trở lên</li>
                                        <li>Định dạng JPG hoặc PNG được khuyến nghị</li>
                                    </ul>
                                </div>
                            </div>
                        </div>

                        <div class="form-group mt-4">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-save"></i> Lưu biến thể
                            </button>
                            <a href="@Url.Action("Index", "ProductVariant", new { productId = Model.product_id })"
                               class="btn btn-secondary">
                                <i class="fa fa-arrow-left"></i> Quay lại
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Preview ảnh trước khi upload
    document.getElementById('imageFile').addEventListener('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            // Kiểm tra định dạng file
            const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/gif'];
            if (!allowedTypes.includes(file.type)) {
                alert('Vui lòng chọn file ảnh (JPG, PNG, GIF)');
                this.value = '';
                return;
            }

            // Kiểm tra kích thước file (tối đa 5MB)
            if (file.size > 5 * 1024 * 1024) {
                alert('Kích thước file không được vượt quá 5MB');
                this.value = '';
                return;
            }

            const reader = new FileReader();
            reader.onload = function(e) {
                document.getElementById('preview').src = e.target.result;
                document.getElementById('imagePreview').style.display = 'block';
            };
            reader.readAsDataURL(file);
        } else {
            document.getElementById('imagePreview').style.display = 'none';
        }
    });

    // Validation form - Tạm thời comment để test upload ảnh
    /*
    document.querySelector('form').addEventListener('submit', function(e) {
        const color = document.querySelector('select[name="color"]').value;
        const price = document.querySelector('input[name="price"]').value;
        const stock = document.querySelector('input[name="stock"]').value;
        const imageFile = document.querySelector('input[name="imageFile"]').files[0];

        if (!color) {
            alert('Vui lòng chọn màu sắc');
            e.preventDefault();
            return;
        }

        if (!price || price <= 0) {
            alert('Vui lòng nhập giá hợp lệ');
            e.preventDefault();
            return;
        }

        if (!stock || stock <= 0) {
            alert('Vui lòng nhập số lượng hợp lệ');
            e.preventDefault();
            return;
        }

        if (!imageFile) {
            alert('Vui lòng chọn ảnh cho biến thể');
            e.preventDefault();
            return;
        }
    });
    */
</script>

<style>
    .card {
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        border: 1px solid rgba(0, 0, 0, 0.125);
    }

    .card-header {
        background-color: #f8f9fa;
        border-bottom: 1px solid rgba(0, 0, 0, 0.125);
    }

    .form-control:focus {
        border-color: #80bdff;
        box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
</style> 